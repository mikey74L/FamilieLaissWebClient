import {enSortDirection} from '../Enum/FamilieLaissEnum';

export class SortCriteria {
  //Members
  propertyName: string;
  localizeName: string;
  direction: enSortDirection;
  displayText: string;
}
